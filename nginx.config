# nginx.conf
events {}

http {
    server {
        listen 80;
        location / {
            # Configure NGINX to serve the React client
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }

        # Add more NGINX configurations as needed
    }
}
