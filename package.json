{
    "name": "social-media-app",
    "version": "1.0.0",
    "main": "",
    "engines": {
        "node": "18.x"
      },
    "scripts": {
        "start": "pwd && node api/build/server.js",
        "install-client": "cd client && npm install && npm run build && cd ..",
        "install-api": "cd api && npm install && npm run build",
        "heroku-postbuild": "npm run install-api"
    }

}