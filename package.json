{
    "name": "social-media-app",
    "version": "1.0.0",
    "main": "",
    "engines": {
        "node": "10.x"
      },
    "scripts": {
        "start": "yarn start --prefix server",
        "install-client": "cd client && yarn install && yarn run build && cd ..",
        "install-server": "cd server && yarn install && yarn run build && cd .. ",
        "heroku-postbuild": "yarn run install-client && yarn run install-server"
    }

}