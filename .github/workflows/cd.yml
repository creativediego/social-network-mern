name: CD Pipeline

on:
  workflow_run:
    workflows:
      - CI Pipeline
    types:
      - completed
      - success
jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: Change directory and checkout
      run: |
          rm -r home/ec2-user/buzzcentral
          mkdir home/ec2-user/buzzcentral
          cd home/ec2-user/buzzcentral
          git init
          git remote add origin https://github.com/creativediego/social-network-mern.git
          git fetch
          git checkout main
        working-directory: /home/ec2-user/buzzcentral

      - name: cur dir
        run: pwd

      - name: List directory contents
        run: ls -a

      # - name: Pull client Docker image from Docker Hub
      #   run: sudo docker pull ${{ secrets.DOCKER_USERNAME }}/social-app:client-production-latest

      # - name: Pull server Docker image from Docker Hub
      #   run: sudo docker pull ${{ secrets.DOCKER_USERNAME }}/social-app:client-production-latest

      # - name: Stop and remove old client container
      #   run: |
      #     sudo docker stop client-production-latest || true
      #     sudo docker rm client-production-latest || true

      # - name: Stop and remove old server container
      #   run: |
      #     sudo docker stop server-production-latest || true
      #     sudo docker rm server-production-latest || true

      # - name: Clean existing 'docker' directory
      #   run: rm -rf "/home/ec2-user/buzzcentral/docker"

      # - name: Run client and server container with Makefile command
      #   run: make -f Makefile run-production DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}
